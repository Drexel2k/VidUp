<UserControl x:Class="Drexel.VidUp.UI.Controls.UploadControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:converters="clr-namespace:Drexel.VidUp.UI.Converters">
    <GroupBox Padding="0" Margin="4">
        <GroupBox.Header>
            <TextBlock Text="{Binding YtTitle, Mode=OneWay}" Margin="2" MaxWidth="610" TextWrapping="Wrap" ></TextBlock>
        </GroupBox.Header>

        <Grid Height="Auto" Width="620" MaxWidth="620"  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Canvas Grid.Row="0" Grid.ColumnSpan="2">
                <Canvas.Background>
                    <ImageBrush ImageSource="{Binding PictureFilePath, Mode=OneWay}" Stretch="Fill" />
                </Canvas.Background>
            </Canvas>

            <!--<CheckBox Grid.Row="0" Grid.Column="0" VerticalAlignment="Top" Style="{StaticResource MaterialDesignLightCheckBox}">
                Check
            </CheckBox>-->

            <Button Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Margin="3"  Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}">
                <materialDesign:PopupBox  StaysOpen="False" Cursor="Hand" >
                    <StackPanel>
                        <Button Content="Reset State" Command="{Binding DataContext.ResetStateCommand, ElementName=UCUploads}" CommandParameter="{Binding Guid}" />
                        <Button Content="Pause" Command="{Binding DataContext.PauseCommand, ElementName=UCUploads}" CommandParameter="{Binding Guid}" />
                        <Button Content="Remove Template" Command="{Binding DataContext.NoTemplateCommand, ElementName=UCUploads}" CommandParameter="{Binding Guid}" />
                        <Separator/>
                        <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, ElementName=UCUploads}" CommandParameter="{Binding Guid}"/>
                    </StackPanel>
                </materialDesign:PopupBox>
            </Button>

            <StackPanel  Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Grid.ColumnSpan="2">
                <Label Content="File: " Width="110" />
                <Label>
                    <TextBlock Text="{Binding FilePath, Mode=OneWay}" TextWrapping="Wrap" MaxWidth="500" />
                </Label>
            </StackPanel>
            <StackPanel  Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                <Label Content="Status: " Width="110" />
                <Label Content="{Binding UploadStatus, Mode=OneWay, Converter={converters:EnumConverter}}" Width="200" />
            </StackPanel>

            <StackPanel  Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                <Label Content="Template: " Width="110" />
                <ComboBox SelectedItem="{Binding Template, Mode=OneWay}" ItemsSource="{Binding DataContext.TemplateList, ElementName=UCUploads}" DisplayMemberPath="Name" Width="190" Tag="{Binding Guid, Mode=OneWay}" DropDownClosed="ComboBox_DropDownClosed" />
            </StackPanel>
            <StackPanel  Grid.Row="3" Grid.Column="0" Orientation="Horizontal">
                <Label Content="Created: " Width="110" />
                <Label Content="{Binding Created, Mode=OneWay}"  Width="200" />
            </StackPanel>
            <StackPanel  Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                <Label Content="Last Modification: " Width="110" />
                <Label Content="{Binding LastModified, Mode=OneWay}"  Width="200"  />
            </StackPanel>
            <StackPanel  Grid.Row="4" Grid.Column="0" Orientation="Horizontal">
                <Label Content="Upload started: " Width="110" />
                <Label Content="{Binding UploadStart, Mode=OneWay}"  Width="200"  />
            </StackPanel>
            <StackPanel  Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
                <Label Content="Upload Ended: " Width="110" />
                <Label Content="{Binding UploadEnd, Mode=OneWay}"  Width="200"  />
            </StackPanel>

        </Grid>


    </GroupBox>
</UserControl>
